# Spine drift, per repo

**Generated by `mlkit spine`. Report-only: this command never writes into a
model repo.** `scripts/sync_spine.py` is the only writer, and it still refuses
to overwrite a file it did not author.

- generated: `2026-08-28T20:59:13+00:00`
- run nonce: `mlkit-20260828T205913Z-fc65ad0ad0c9`
- spine: `/Users/david/Downloads/Claude Code/resilient-mlkit/spine` at mlkit `a04c60c1d9746afa3a3ea0dcd2e73945e1458c15`
- canonical files compared per repo: **6**
- verdicts: **DRIFTED** 16, **IN-SYNC** 32

| verdict | meaning |
|---|---|
| `IN-SYNC` | deployed copy is byte-identical to the spine |
| `DRIFTED` | carries the `CANONICAL` banner but its bytes have moved; the next sync reverts it |
| `ABSENT` | no deployed copy at all — the spine is not in force here |
| `UNCLAIMED` | a file without the banner occupies the canonical filename; the syncer will not touch it |
| `NO-SPINE-SOURCE` | the spine itself has no such file — a defect in this repo, not in that one |

## Per repo

| repo | branch | CLAUDE.md | `docs/DATA_POLICY.md` | `docs/SELECTION.md` | `docs/READINESS.md` | `docs/DECISION_VALIDITY.md` | `docs/RUN_ECONOMICS.md` |
|---|---|---|---|---|---|---|---|
| choco | `review/observed-labels-fixes` | **DRIFTED** (31 lines) | in sync | in sync | **DRIFTED** (66 lines) | in sync | in sync |
| arabica | `feat/observed-panel-and-fabrication-gates` | **DRIFTED** (31 lines) | in sync | in sync | **DRIFTED** (66 lines) | in sync | in sync |
| fray | `feat/r6-serving-and-uncertainty` | **DRIFTED** (31 lines) | in sync | in sync | **DRIFTED** (66 lines) | in sync | in sync |
| torrent | `feat/r6-attributes-and-scale` | **DRIFTED** (31 lines) | in sync | in sync | **DRIFTED** (66 lines) | in sync | in sync |
| chokepoint | `feat/r6-horizon-ladder-and-uq` | **DRIFTED** (31 lines) | in sync | in sync | **DRIFTED** (66 lines) | in sync | in sync |
| surge | `feat/observed-corpus-and-fabrication-gates` | **DRIFTED** (31 lines) | in sync | in sync | **DRIFTED** (66 lines) | in sync | in sync |
| triage | `e028-decision` | **DRIFTED** (31 lines) | in sync | in sync | **DRIFTED** (66 lines) | in sync | in sync |
| blackout | `e021-decision` | **DRIFTED** (31 lines) | in sync | in sync | **DRIFTED** (66 lines) | in sync | in sync |

## What moved

- **choco / `CLAUDE.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 31 line(s) away from spine/CLAUDE.md; the next sync would revert it
  - spine `8526df9dc9c454c2…` vs deployed `5aa52879d78cbcad…`
  - `-## Credentials are not blockers`
  - `-`
  - `-7. **A missing API key does not stop the work.** If a source is commercially`
  - `-   open and permissive, wire the ingest path properly, exercise it as far as`
  - `-   the credential boundary, and raise`
  - `-   `resilient_mlkit.CredentialRequired(name, detail, evidence)` from the`
- **choco / `docs/READINESS.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 66 line(s) away from spine/docs/READINESS.md; the next sync would revert it
  - spine `aed512c6a0498f0e…` vs deployed `00838426cbb8a011…`
  - `-Eleven checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `+Nine checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `-| 2 | **R10** `FABRICATED_DEFAULTS` | A pure `ast` walk — no imports, no data, no network — so it is the cheapest check after R9, and decisive in the widest se`
  - `-| 3 | **R11** `FABRICATED_TARGETS` | The same kind of walk, so the same cost. It runs before **R5** specifically: R5 counts rows by the provenance field R11 ad`
  - `-| 4 | **R1** `CHECKPOINT_PROVENANCE` | Static. Answers "do we know what these weights are" before anything loads them. |`
  - `-| 5 | **R2** `OVERFIT_ONE_BATCH` | The cheapest check that can prove the model is wired up at all. A model that cannot overfit one batch has a defect no amount`
- **arabica / `CLAUDE.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 31 line(s) away from spine/CLAUDE.md; the next sync would revert it
  - spine `8526df9dc9c454c2…` vs deployed `5aa52879d78cbcad…`
  - `-## Credentials are not blockers`
  - `-`
  - `-7. **A missing API key does not stop the work.** If a source is commercially`
  - `-   open and permissive, wire the ingest path properly, exercise it as far as`
  - `-   the credential boundary, and raise`
  - `-   `resilient_mlkit.CredentialRequired(name, detail, evidence)` from the`
- **arabica / `docs/READINESS.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 66 line(s) away from spine/docs/READINESS.md; the next sync would revert it
  - spine `aed512c6a0498f0e…` vs deployed `00838426cbb8a011…`
  - `-Eleven checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `+Nine checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `-| 2 | **R10** `FABRICATED_DEFAULTS` | A pure `ast` walk — no imports, no data, no network — so it is the cheapest check after R9, and decisive in the widest se`
  - `-| 3 | **R11** `FABRICATED_TARGETS` | The same kind of walk, so the same cost. It runs before **R5** specifically: R5 counts rows by the provenance field R11 ad`
  - `-| 4 | **R1** `CHECKPOINT_PROVENANCE` | Static. Answers "do we know what these weights are" before anything loads them. |`
  - `-| 5 | **R2** `OVERFIT_ONE_BATCH` | The cheapest check that can prove the model is wired up at all. A model that cannot overfit one batch has a defect no amount`
- **fray / `CLAUDE.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 31 line(s) away from spine/CLAUDE.md; the next sync would revert it
  - spine `8526df9dc9c454c2…` vs deployed `5aa52879d78cbcad…`
  - `-## Credentials are not blockers`
  - `-`
  - `-7. **A missing API key does not stop the work.** If a source is commercially`
  - `-   open and permissive, wire the ingest path properly, exercise it as far as`
  - `-   the credential boundary, and raise`
  - `-   `resilient_mlkit.CredentialRequired(name, detail, evidence)` from the`
- **fray / `docs/READINESS.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 66 line(s) away from spine/docs/READINESS.md; the next sync would revert it
  - spine `aed512c6a0498f0e…` vs deployed `00838426cbb8a011…`
  - `-Eleven checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `+Nine checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `-| 2 | **R10** `FABRICATED_DEFAULTS` | A pure `ast` walk — no imports, no data, no network — so it is the cheapest check after R9, and decisive in the widest se`
  - `-| 3 | **R11** `FABRICATED_TARGETS` | The same kind of walk, so the same cost. It runs before **R5** specifically: R5 counts rows by the provenance field R11 ad`
  - `-| 4 | **R1** `CHECKPOINT_PROVENANCE` | Static. Answers "do we know what these weights are" before anything loads them. |`
  - `-| 5 | **R2** `OVERFIT_ONE_BATCH` | The cheapest check that can prove the model is wired up at all. A model that cannot overfit one batch has a defect no amount`
- **torrent / `CLAUDE.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 31 line(s) away from spine/CLAUDE.md; the next sync would revert it
  - spine `8526df9dc9c454c2…` vs deployed `5aa52879d78cbcad…`
  - `-## Credentials are not blockers`
  - `-`
  - `-7. **A missing API key does not stop the work.** If a source is commercially`
  - `-   open and permissive, wire the ingest path properly, exercise it as far as`
  - `-   the credential boundary, and raise`
  - `-   `resilient_mlkit.CredentialRequired(name, detail, evidence)` from the`
- **torrent / `docs/READINESS.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 66 line(s) away from spine/docs/READINESS.md; the next sync would revert it
  - spine `aed512c6a0498f0e…` vs deployed `00838426cbb8a011…`
  - `-Eleven checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `+Nine checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `-| 2 | **R10** `FABRICATED_DEFAULTS` | A pure `ast` walk — no imports, no data, no network — so it is the cheapest check after R9, and decisive in the widest se`
  - `-| 3 | **R11** `FABRICATED_TARGETS` | The same kind of walk, so the same cost. It runs before **R5** specifically: R5 counts rows by the provenance field R11 ad`
  - `-| 4 | **R1** `CHECKPOINT_PROVENANCE` | Static. Answers "do we know what these weights are" before anything loads them. |`
  - `-| 5 | **R2** `OVERFIT_ONE_BATCH` | The cheapest check that can prove the model is wired up at all. A model that cannot overfit one batch has a defect no amount`
- **chokepoint / `CLAUDE.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 31 line(s) away from spine/CLAUDE.md; the next sync would revert it
  - spine `8526df9dc9c454c2…` vs deployed `5aa52879d78cbcad…`
  - `-## Credentials are not blockers`
  - `-`
  - `-7. **A missing API key does not stop the work.** If a source is commercially`
  - `-   open and permissive, wire the ingest path properly, exercise it as far as`
  - `-   the credential boundary, and raise`
  - `-   `resilient_mlkit.CredentialRequired(name, detail, evidence)` from the`
- **chokepoint / `docs/READINESS.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 66 line(s) away from spine/docs/READINESS.md; the next sync would revert it
  - spine `aed512c6a0498f0e…` vs deployed `00838426cbb8a011…`
  - `-Eleven checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `+Nine checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `-| 2 | **R10** `FABRICATED_DEFAULTS` | A pure `ast` walk — no imports, no data, no network — so it is the cheapest check after R9, and decisive in the widest se`
  - `-| 3 | **R11** `FABRICATED_TARGETS` | The same kind of walk, so the same cost. It runs before **R5** specifically: R5 counts rows by the provenance field R11 ad`
  - `-| 4 | **R1** `CHECKPOINT_PROVENANCE` | Static. Answers "do we know what these weights are" before anything loads them. |`
  - `-| 5 | **R2** `OVERFIT_ONE_BATCH` | The cheapest check that can prove the model is wired up at all. A model that cannot overfit one batch has a defect no amount`
- **surge / `CLAUDE.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 31 line(s) away from spine/CLAUDE.md; the next sync would revert it
  - spine `8526df9dc9c454c2…` vs deployed `5aa52879d78cbcad…`
  - `-## Credentials are not blockers`
  - `-`
  - `-7. **A missing API key does not stop the work.** If a source is commercially`
  - `-   open and permissive, wire the ingest path properly, exercise it as far as`
  - `-   the credential boundary, and raise`
  - `-   `resilient_mlkit.CredentialRequired(name, detail, evidence)` from the`
- **surge / `docs/READINESS.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 66 line(s) away from spine/docs/READINESS.md; the next sync would revert it
  - spine `aed512c6a0498f0e…` vs deployed `00838426cbb8a011…`
  - `-Eleven checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `+Nine checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `-| 2 | **R10** `FABRICATED_DEFAULTS` | A pure `ast` walk — no imports, no data, no network — so it is the cheapest check after R9, and decisive in the widest se`
  - `-| 3 | **R11** `FABRICATED_TARGETS` | The same kind of walk, so the same cost. It runs before **R5** specifically: R5 counts rows by the provenance field R11 ad`
  - `-| 4 | **R1** `CHECKPOINT_PROVENANCE` | Static. Answers "do we know what these weights are" before anything loads them. |`
  - `-| 5 | **R2** `OVERFIT_ONE_BATCH` | The cheapest check that can prove the model is wired up at all. A model that cannot overfit one batch has a defect no amount`
- **triage / `CLAUDE.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 31 line(s) away from spine/CLAUDE.md; the next sync would revert it
  - spine `8526df9dc9c454c2…` vs deployed `5aa52879d78cbcad…`
  - `-## Credentials are not blockers`
  - `-`
  - `-7. **A missing API key does not stop the work.** If a source is commercially`
  - `-   open and permissive, wire the ingest path properly, exercise it as far as`
  - `-   the credential boundary, and raise`
  - `-   `resilient_mlkit.CredentialRequired(name, detail, evidence)` from the`
- **triage / `docs/READINESS.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 66 line(s) away from spine/docs/READINESS.md; the next sync would revert it
  - spine `aed512c6a0498f0e…` vs deployed `00838426cbb8a011…`
  - `-Eleven checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `+Nine checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `-| 2 | **R10** `FABRICATED_DEFAULTS` | A pure `ast` walk — no imports, no data, no network — so it is the cheapest check after R9, and decisive in the widest se`
  - `-| 3 | **R11** `FABRICATED_TARGETS` | The same kind of walk, so the same cost. It runs before **R5** specifically: R5 counts rows by the provenance field R11 ad`
  - `-| 4 | **R1** `CHECKPOINT_PROVENANCE` | Static. Answers "do we know what these weights are" before anything loads them. |`
  - `-| 5 | **R2** `OVERFIT_ONE_BATCH` | The cheapest check that can prove the model is wired up at all. A model that cannot overfit one batch has a defect no amount`
- **blackout / `CLAUDE.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 31 line(s) away from spine/CLAUDE.md; the next sync would revert it
  - spine `8526df9dc9c454c2…` vs deployed `5aa52879d78cbcad…`
  - `-## Credentials are not blockers`
  - `-`
  - `-7. **A missing API key does not stop the work.** If a source is commercially`
  - `-   open and permissive, wire the ingest path properly, exercise it as far as`
  - `-   the credential boundary, and raise`
  - `-   `resilient_mlkit.CredentialRequired(name, detail, evidence)` from the`
- **blackout / `docs/READINESS.md` — DRIFTED**. deployed copy carries the CANONICAL banner but has moved 66 line(s) away from spine/docs/READINESS.md; the next sync would revert it
  - spine `aed512c6a0498f0e…` vs deployed `00838426cbb8a011…`
  - `-Eleven checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `+Nine checks. `mlkit check --phase readiness` runs them **in the order below,`
  - `-| 2 | **R10** `FABRICATED_DEFAULTS` | A pure `ast` walk — no imports, no data, no network — so it is the cheapest check after R9, and decisive in the widest se`
  - `-| 3 | **R11** `FABRICATED_TARGETS` | The same kind of walk, so the same cost. It runs before **R5** specifically: R5 counts rows by the provenance field R11 ad`
  - `-| 4 | **R1** `CHECKPOINT_PROVENANCE` | Static. Answers "do we know what these weights are" before anything loads them. |`
  - `-| 5 | **R2** `OVERFIT_ONE_BATCH` | The cheapest check that can prove the model is wired up at all. A model that cannot overfit one batch has a defect no amount`
