# E-M43 / E-M44 — RESULTS

Every reading here was produced by driving, against the acceptance rules and
arms fixed in `E_M43_M44_GATE_DEFECT_PREREGISTRATION.md` before any change to
`src/`. Nothing in this file was reconstructed from a diff.

---

# E-M43 — the exit status now carries every verdict the command prints

## The premise, corrected by measurement first

| clause as handed over | measured on `main` `912853df` |
|---|---|
| a one-character `entries_sha256` flip prints INVALID and returns **0** | **NOT REPRODUCED.** Arm **C2**: rc **1**. The 0 belongs to a PIPELINE — arm **C2P**, the same command into `head`, shell reports **0**. Withdrawn as a tool defect; pinned by a test as behaviour that must not regress. |
| matching NO repository exits 0 printing nothing | **REPRODUCED.** Arm **C8b** (`cli.py` at `origin/main`): rc **0**, 0 lines on stdout, 0 on stderr. |
| an UNSIGNED allowlist | **REPRODUCED**, and it was not in the evidence handed over. Arm **C8a**: rc **0**, output `arabica: 1 entries, UNSIGNED`. |

## The control arms, driven

`reports/E_M43_ALLOWLIST_EXIT_ARMS.json`, written by
`scripts/e_m43_allowlist_exit_drive.py`, read back by
`tests/test_e_m43_allowlist_exit.py`.

| arm | condition | required | READ |
|---|---|---|---|
| **C1** | two repos, valid and signed | rc 0 | **0** ✔ |
| **C2** | `entries_sha256` flipped, mutation verified | rc 1, names the digest | **1**, `signature.entries_sha256 does not match the entries it covers` ✔ |
| **C2P** | C2's command piped into `head` | 0 — the pipeline's, not the command's | **0** ✔ |
| **C3** | `signature.signed: false` | rc 1, says UNSIGNED | **1**, `UNSIGNED — these determinations have not been ratified` ✔ |
| **C4** | `docs/allowlist.yaml` absent | rc 1, says MISSING | **1** ✔ |
| **C5** | entry with no `retrieval_date`, correctly signed | rc 1, names the defect | **1**, `retrieval_date is missing` ✔ |
| **C6** | `--root` with no `resilient-*` under it | rc 2, refusal names the root | **2**, `no portfolio repos found under <root> … REFUSED, not a pass` ✔ |
| **C7** | `--repo choco` where choco is not checked out | rc 2 | **2**, and the refusal names `--repo choco` ✔ |
| **C8a** | **NOT-DEAD**: `cli.py` at `origin/main`, C3 re-driven | rc 0 — the defect | **0** ✔ |
| **C8b** | **NOT-DEAD**: `cli.py` at `origin/main`, C6 re-driven | rc 0, no output — the defect | **0**, 0/0 lines ✔ |

Every arm ran in a subprocess that first asserted `resilient_mlkit.__file__`
resolves inside the package tree the arm names, exiting 9 otherwise; a venv
with an editable install would otherwise have served one tree to all ten arms.
Every fixture mutation was applied in Python and refuses a no-op: C2 records
`recorded_prefix` and `computed_prefix` and asserts they differ.

**The artifact names no directory.** C6's refusal quotes the root by
construction; the record keeps `stderr_named_the_root: true` and
`root_token_length`, and `machine_paths_in_text` runs over the finished file
before it is written.

## The inventory — every subcommand read for the same two shapes

| subcommand | printed failure with exit 0? | silent no-op on an empty match? | action |
|---|---|---|---|
| `allowlist verify` | **YES** — UNSIGNED | **YES** — nothing printed, rc 0 | **FIXED**: 1 and 2 |
| `notice` | **YES** — `REFUSED — NOTICE.md exists … not generated by mlkit` | no (already exit 2) | **FIXED**: rc 1 when any NOTICE was not written. A repo with no allowlist stays 0 — there is no obligation to render, and R9 escalates that state rather than failing it |
| `keys` | no | **YES** — printed "Nothing in the portfolio is waiting on a key" having read nothing | **FIXED**: rc 2 |
| `check` | no — `portfolio.exit_code`, and `--allow-dirty` refuses with 2 | no | none |
| `portfolio` (`cmd_fleet`) | no — 1 when a declared main artifact did not resolve | no | none |
| `spine` | no — 1 ABSENT/UNCLAIMED, 3 DRIFTED/NO_SPINE_SOURCE | no | none |
| `env` | no — 1 if any repo is UNMEASURABLE | no | none |
| `ancestry` | no — 0 contained, 1 not, 2 the question could not be asked | n/a (its own "exactly one repo" guard, exit 2) | none |
| `identity --verify` | no — 0 MATCH, 3 MISMATCH, 1 otherwise | n/a | none |
| `register --check-fleet` | no — 0/1, and `REGISTER_REFUSED_EXIT` 2 is already "measured nothing" | n/a | none |
| `register --verify-artifact` | no — 0/1/2 | n/a | none |

The empty-selection guard existed in five separate copies and was absent from
the two commands that needed it; it is one definition now
(`_refuse_empty_selection` / `NOTHING_MATCHED_EXIT`) used by all seven
repo-walking commands, and one parametrised test asserts all seven.

## The fleet, before and after — ZERO rows moved, as preregistered

Eight adopter clones at the remote mains listed in the preregistration §5, five
phases each (`triage`, `selection`, `readiness`, `decision`, `economics`), ONE
interpreter with `PYTHONPATH` switched between `origin/main`'s package tree and
the branch's, `resilient_mlkit.__file__` asserted inside the named tree at the
head of each drive (exit 9 otherwise), every clone `git reset --hard` and
`git clean -fdx` before every phase, arms SEQUENTIAL, load1 2.2–3.1 against
`hw.ncpu` 10 at both ends of every span.

```
rows BEFORE 272    rows AFTER 272
added 0    removed 0    STATUS moved 0    reason moved 0
```

**The instrument's determinism was measured, not assumed.** Two independent
drives of `origin/main`, an hour apart, agree on all 272 rows — status and
reason — so a zero difference in the A/B is a statement about the change and
not about a drive that cannot see anything.

## The suite — ONE clone, arms SEQUENTIAL, failure sets by name

```
arm     ref          head      tree      result                        elapsed
MAIN    origin/main  912853d   7fd69897  1411 passed, 3 skipped, rc 0    106s
BRANCH  e-m43        <head>    8011351d  1438 passed, 3 skipped, rc 0    106s

failures BY NAME:  MAIN 0   BRANCH 0
only-on-branch NONE      only-on-main NONE      BOTH sets EMPTY
+27 passing = the 27 tests added by this branch

skips BY NODE ID, identical in both arms, all three for the same reason:
  tests/test_torrent_model_of_record.py::test_tr3_both_real_rows_report_a_measured_model_of_record
  tests/test_torrent_model_of_record.py::test_tr3_the_declared_record_is_committed_in_the_real_repo
  tests/test_torrent_model_of_record.py::test_tr3_the_value_is_the_bar_the_sibling_row_measures_against
  reason: resilient-torrent is not checked out beside this clone

ruff 0.16.5  src tests scripts   : main 0  ->  branch 0
mypy 2.3.1   src/resilient_mlkit : main 0  ->  branch 0 (40 files)
```

`pytest tests -q --timeout=1800 -o timeout_method=signal`, identical flags in
both arms, ONE clone with its own venv (`pip install -e ".[test]"`), and
`resilient_mlkit.__file__` asserted to resolve inside it at the head of each
arm — the shared venv on this host carries an editable path to a different
clone and would silently have measured that one.

## The version — MINOR, decided by the drive

A CLI surface changes and no check is touched; the fleet drive moved zero rows.
**v2.2.0.** The major clause — v3.0.0 had any verdict moved — was written into
the preregistration §4 before the drive and did not fire.
